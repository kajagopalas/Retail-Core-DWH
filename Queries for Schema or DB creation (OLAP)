Create Database SALES_ANALYTICS_DB
Use SALES_ANALYTICS_DB

Create Table DIM_CUSTOMER (
CUSTOMER_ID Varchar(20) Primary Key,
CUSTOMER_NAME Varchar(30),
CITY_ID Varchar(25),
DOB Date)



Alter Table DIM_CUSTOMER
Add Constraint FK_CITY_ID foreign key (CITY_ID) References DIM_CITY (CITY_ID)


Create Table DIM_CITY (
CITY_ID Varchar(25) Primary Key,
CITY_NAME Varchar(25),
REGION_ID Varchar(25))


Alter Table DIM_CITY
Add Constraint FK_REGION_ID foreign key (REGION_ID) References DIM_REGION (REGION_ID)

Create Table DIM_REGION (
REGION_ID Varchar(25) Primary Key,
REGION_NAME Varchar(25),
COUNTRY Varchar(25))

Create Table DIM_STORE (
STORE_ID Varchar(30) Primary Key,
STORE_NAME Varchar(30),
STORE_CITY_ID Varchar(10),
STORE_REGION_ID Varchar(20))

Create Table DIM_PRODUCT (
PRODUCT_ID Varchar(20) Primary Key,
PRODUCT_NAME VarchAr(30),
CATEGORY_ID Varchar(30))

Alter Table DIM_PRODUCT
Add Constraint FK_CATEGORY_ID foreign key (CATEGORY_ID) References DIM_CATEGORY (CATEGORY_ID)

Create Table DIM_CATEGORY (
CATEGORY_ID Varchar(30) Primary Key,
CATEGORY_NAME Varchar(30),
DEPARTMENT_ID Varchar(30)) 

Alter Table DIM_CATEGORY
Add Constraint FK_DEPARTMENT_ID foreign key (DEPARTMENT_ID) References DIM_DEPARTMENT (DEPARTMENT_ID)


Create Table DIM_DEPARTMENT (
DEPARTMENT_ID Varchar(30) Primary Key,
DEPARTMENT_NAME Varchar(30))


Create Table DIM_DATE  (
DATE_ID Varchar(30) Primary Key,
CALENDAR_DATE Date,
Year Varchar(20),
Month Varchar(20),
Day_Of_Month Varchar(20),
Week_Of_Year Varchar(20),
Weekday_Name Varchar(20))


Create Table FACT_SALES  (
SALES_ID Varchar(30) Primary Key,
CUSTOMER_ID Varchar(20) foreign key references DIM_CUSTOMER (CUSTOMER_ID),
STORE_ID Varchar(30) foreign key references DIM_STORE (STORE_ID),
PRODUCT_ID Varchar(20) foreign key references DIM_PRODUCT (PRODUCT_ID),
DATE_ID Varchar(30) foreign key references DIM_DATE(DATE_ID),
SALE_DATE Date,
QUANTITY Int,
UNIT_PRICE Decimal(10,2),
DISCOUNT Decimal(10,2),
TOTAL_AMOUNT Decimal(10,2),
PAYMENT_MODE Varchar(20))

